node_modules
dist
.DS_Store
*.log
.env
.env.local
.env.*.local
.vite
.vercel
*.tsbuildinfo

# Capacitor — native build artifacts & copied web assets are regenerated by `npx cap sync`.
# (android/.gitignore and ios/.gitignore handle most of it; these are extra safety.)
android/app/src/main/assets/public
ios/App/App/public
android/.gradle
android/app/build
android/build
ios/App/build
ios/DerivedData

# Android app signing — NEVER commit these.
*.keystore
*.jks
keystore.properties
android/keystore.properties
android/key.properties

.env*.local
